# Runtime orchestration logs (folder is tracked via .gitkeep)
logs/*.log
node_modules/

# Internal agent workspace file (never committed)
.abacus.donotdelete

# Claim-Node Layer — generated runtime artifacts (directories tracked via .gitkeep)
claims/*
!claims/.gitkeep
disputes/*
!disputes/.gitkeep
reports/*
!reports/.gitkeep
